# torch/torchvision: install separately with the CUDA build that matches your driver, e.g.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
torch>=2.3
torchvision>=0.18
numpy>=1.24
pandas>=2.0
scipy>=1.10
pyyaml>=6.0
matplotlib>=3.7
torchmetrics>=1.3
lpips>=0.1.4
pytest>=7.0